# Dockerized-React-Nodejs

## Start up project with docker-compose

To start up the project run:

```
docker-compose up --build
```

in the root folder

## Todo

- [ ] Configure a database container
- [ ] Connect OAuth flow
